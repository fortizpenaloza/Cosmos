initialize-release
initializeIdentifiedBy: anIdentifier named: aName createdOn: aDateTime 
	
	identifier := anIdentifier.
	name := aName.
	dateTime := aDateTime.
	
	commands := OrderedCollection new
	
	